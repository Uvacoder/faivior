import Styled from 'styled-components'
import { maxQuery } from '../../../helpers/media'

export default Styled.div`
    div.modal--content {
        max-height: 400px;
        padding: 1em 2em;
    }
`
